let frutas =['mango','platano','manzana','fresa'];

// En este desafío vas a recibir un parámetro arraySecreto en la función solution. 
// Debes retornar true si el primer elemento del arraySecreto es de tipo string y debes retornar 
// false si es de cualquier otro tipo.

if (typeof(arraySecreto[0]) == String){
    return true;
    }
    else{
        return false;
    }
   